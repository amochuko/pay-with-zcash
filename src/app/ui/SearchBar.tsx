export default function SearchBar(){
    return (
      <div>

        <input
          type="text"
          placeholder="Search..."
          className="uppercase border border-white/[.45] p-4 max-w-2xl"
        />
      </div>
    );
}
